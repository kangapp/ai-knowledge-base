# 《和 AI 共创项目的指南》共创记录

## 已确认决定

| 编号 | 决定 | 确认依据 |
| --- | --- | --- |
| D-01 | 面向有基础编程能力的独立开发者。 | 用户选择 A。 |
| D-02 | 以从零创建项目为主线，已有项目的差异在必要处补充。 | 用户选择 A。 |
| D-03 | 正文分为启动项目共创、从想法到持续运行、项目共创示例三部分；详细检查项按需参考。 | 用户选择 A。 |
| D-04 | 采用包含协作方式、阶段推进和交付要求的完整启动提示词。 | 第一部分草稿后，用户选择 A。 |
| D-05 | 采用需求与 Spec、技术选型、编码与测试、部署上线、运维迭代五阶段，列明分工、产物和完成条件。 | 第二部分草稿后，用户选择 A。 |
| D-06 | 采用个人收藏工具的教学案例，以应用和数据库同机部署的决定贯穿编码、上线和运维。 | 第三部分草稿后，用户选择 A。 |
| D-07 | 将已确认内容整合成 HTML 草稿并审校。 | 同上轮选择 A。 |
| D-08 | 补充项目 Prompt 工具箱，结合 OpenAI 的提示词参考。 | 用户明确提出补充要求。 |
| D-09 | 包含三部分正文、阶段检查与记录、项目 Prompt 工具箱的当前 HTML 版本正式定稿。 | 工具箱补充与审校完成后，用户选择 A（定稿）。 |
| D-10 | 按仓库现有 CI/CD 流程将定稿内容部署到服务器。 | 用户明确要求“部署到服务器”。 |

## 产物

- `README.md`：三部分正文，渲染为 `index.html`。
- `reference.md`：按需参考的阶段检查和记录，渲染为 `reference.html`。
- `prompts.md`：按阶段折叠展示的 10 个可复制模板与官方依据，渲染为 `prompts.html`。
- `topic.yaml`：分析模块注册与精简导航。

## 假设与建议

- 教学案例的预算、访问范围和部署选择仅用于解释方法，不是本知识库的新功能需求。
- 具体框架、存储产品、访问控制和备份安排留给真实项目按约束决定，不在通用指南中固定。
- 审校补充表格滚动说明、示意图省略范围和验证状态说明，并将首次收藏、取消后刷新的验收写具体，不改变已确认的正文方向。

## 待定事项

- 无待确认的内容事项，最终版本已获确认。
- 服务器发布已获授权，实际部署结果以仓库 GitHub Actions 中对应提交的 Deploy 运行记录及线上验收为准。

## 本次审校与验证

- 已核对三部分正文与分节确认内容，补全案例中首次收藏和取消收藏的验收表达。
- `uv run pytest tests/test_site_builder.py -q`：21 项通过。
- 生成页的 19 个本地链接与资源引用检查通过，目标文件和锚点均存在；分析列表包含新专题入口。
- 浏览器检查 1280px 桌面与 390px 手机布局，页面无横向溢出；宽表在自身区域内滚动，手机可查看右侧列。
- 提示词复制成功；参考导航展开后首页入口约 43px 高；参考页往返与部署示意图渲染正常。
- 以上是指南页面的验证结果；收藏工具案例中的工程验证仍全部待执行。

## Prompt 工具箱补充验证

- 根据 OpenAI 的提示结构、推理模型提示建议和编程协作示例，新增 P01–P10；在页末说明原则与来源的对应关系。
- 工具箱模板默认折叠，10 个复制目标和反馈区域均存在；3 个生成页面的 37 个本地链接与资源引用检查通过。
- `uv run pytest tests/test_site_builder.py -q`：21 项通过。
- 浏览器确认从首页导航进入工具箱；P08 展开和复制成功，390px 宽度下提示词换行正常，页面与代码区无横向溢出。
- 最终版本已获用户确认；上述验证记录适用于本次定稿内容。
