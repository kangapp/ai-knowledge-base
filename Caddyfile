{
    # 本地开发禁用 HTTPS
    admin off
}

:80 {
    root * /srv
    file_server
    header Cache-Control "public, max-age=3600"
    handle /api/* {
        reverse_proxy pipeline:8000
    }
    encode gzip
}